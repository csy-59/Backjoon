#include <iostream>
#include <vector>
#include <utility>
using namespace std;

int v;
vector<pair<int, int>> tree[100001];

vector<int> arr;

int main()
{
	arr.push_back(1);
	arr.erase(arr.begin() + 0);

	return 0;

	ios::sync_with_stdio(false);
	cin.tie(nullptr);
	cout.tie(nullptr);

	cin >> v;

	//tree[0].erase(1)
	//
	//int vertex, time, next;
	//for (int i = 0; i < v; ++i)
	//{
	//	bool isConnected[100001] = { false };
	//	cin >> vertex;
	//
	//	do
	//	{
	//		cin >> time;
	//		if (time == -1)
	//			break;
	//
	//
	//	} while (1);
	//}

	return 0;
}